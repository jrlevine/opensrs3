# opensrs3
Python library for OpenSRS API

This is an updated version of the library provided by FatBox in 2011.

The changes are small, a few to make it compatible with modern python3,
and a __del__() routine that closes the OpenSRS connection.

